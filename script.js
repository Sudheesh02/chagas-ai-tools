function predict() {
  const smiles = document.getElementById('smiles-input').value;
  // Placeholder: Replace with actual model prediction later
  document.getElementById('result').innerHTML = `Prediction for ${smiles}: -4.85 (example)`;
  alert("We'll connect this to your model next!");
}